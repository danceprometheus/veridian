{
  "name": "veridian",
  "version": "1.1.0",
  "description": "HVN: Veridian - A Metaverse",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "typecheck": "node --check src/*.js && node --check server/*.js",
    "test": "node --test",
    "test:server": "node --test server/*.test.js"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.39.0",
    "colyseus.js": "^0.15.0",
    "three": "^0.160.0"
  },
  "devDependencies": {
    "vite": "^5.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
